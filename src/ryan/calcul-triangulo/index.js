const comprimento = 20;
const largura = 10;
// const perimetro = 15;

function calcularArea(largura, comprimento, perimetro) {
  return comprimento * largura;
}
console.log(calcularArea(largura, comprimento));

function calcularPerimetro(largura, comprimento, perimetro) {
  return comprimento * largura;
}
